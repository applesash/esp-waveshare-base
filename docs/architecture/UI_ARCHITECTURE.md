# UI Architecture

Each feature has one screen model, ViewModel, workflow, navigation definition, validation model, permission model, and theme. LVGL, embedded Web, and local preview are renderers. Responsive layout differences are permitted, but information, state, actions, validation, permissions, and workflow must remain equivalent.
