# Test Architecture

Use unit, component, service, integration, simulation, renderer-contract, hardware, OTA rollback, storage-failure, power-cycle, security, and acceptance tests. Physical validation requires exact-SKU evidence.
